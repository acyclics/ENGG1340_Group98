#include <string>
#include <vector>
int N; // number of customers

/*
ConstantMatrix : Store all the constant for calculation
InitializeConstantMatrix : initialize the constant matrix
*/
void InitializeConstantMatrix();
